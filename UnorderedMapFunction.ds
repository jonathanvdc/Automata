using std;

namespace Automata
{
    /// <summary>
    /// Defines a pure function that uses an unordered_map under the hood.
    /// </summary>
    public class UnorderedMapFunction<TSource, TTarget> : IFunction<TSource, TTarget>
    {
        public const this(set unordered_map<TSource, TTarget> Map);
        
        public unordered_map<TSource, TTarget> Map { const get; private set; }
        
        public const TTarget Apply(TSource Value)
        {
            return Map[Value];
        }
    }
}